# 2026-09-01

## Screening runs
- Screened refno 2600827001 (4 candidates) and 260806012 (3 candidates, "Project Officer").
  Reports in `Desktop/workbuddy-cv-screen/<refno>/`.

## Routing change: "screen refno <digits>" now goes through WebBridge
- User wants `pls screen refno <digits>` to open https://jes-web-demo.vercel.app/ in their
  real browser via **Kimi WebBridge** and demo the human flow (type refno into the list-page
  filter -> click the row's **View** link -> download CVs), then run the pipeline.
- The `webridge-collect` skill already implemented this; the gap was only the routing rule.
- Updated `AGENTS.md` step 2: refno/URL now defaults to
  `run_webridge_collect.py "<refno-or-url>" --driver webbridge`.
  `run_jas_import.py` is kept for HR-exported folders, or `--driver http` when the daemon is down.
- Added the same pointer to `.codex/skills/webridge-collect/SKILL.md` and
  `.codex/skills/jas-import/SKILL.md` (command table + note).

## WebBridge notes
- Daemon: `http://127.0.0.1:10086`, check with `curl -s -X POST http://127.0.0.1:10086/status`.
  Start: `& "$env:USERPROFILE\.kimi-webbridge\bin\kimi-webbridge.exe" start`.
- Daemon was already running (v1.11.6, extension connected). Default session `jes-demo-screen`.
- `demo_mode.json` (repo root, `enabled: true`) auto-supplies `--base-url`,
  `--allow-host`, `--no-cookie` — do not pass them by hand.
- Verified the human flow is real, not a fallback: evaluate returned
  `{typed: True, clicked: True, text: "View", href: .../records.html?refno=260806012}`.
  (The View href equals the records URL, so target equality alone does NOT prove fallback.)
- Collected data lands in `data/jes_webridge/<refno>/` (records.html + cvs/<appno>.pdf +
  `_webridge-manifest.json`).
