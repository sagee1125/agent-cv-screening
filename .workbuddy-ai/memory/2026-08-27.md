# 2026-08-27 Work Log

## JAS offline screening pipeline run (mock data)

Ran the full three-stage pipeline on mock JAS data at `C:\Users\User\Desktop\jasweb\mock`:

1. **jas-import** (offline parse): Used `run_jas_import.py parse-list` and `parse-job` to parse `list.html` and `records.html`. Output: `catalog.json` (1 job: refno 260818001, Project Associate at IHERD) and `job-detail.json` (JD text + 2 candidates: appno 123456 status S, appno 654321 status TBC). PII properly stripped — only appno/status/cv_url retained.

2. **run_jas_screening** (offline mode `--jas-dir`): cvs/ folder existed with 2 PDFs (123456.pdf, 654321.pdf). Pipeline ran with `--engine matching --skip-reports`. Results:
   - Candidate 123456: rank 1, match_score 45.31, fit_band low
   - Candidate 654321: rank 2, match_score 0.0, fit_band low
   - Generated: jas-manifest.json, jd.txt, jd-parse.json, extracted/detail JSONs, pipeline-stdout.json

3. **host-envelope** (projection): Projected pipeline stdout through `run_host_envelope.py --tool screen_refno`. Output: `host-envelope.json` — PII-free whitelist envelope with ranking keyed by appno only. All paths, names, score dumps stripped.

All outputs in `data/jas_out/`. Python: project venv at `venv/Scripts/python.exe` (3.12.0). No JAS login was needed — fully offline.
