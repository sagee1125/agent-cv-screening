interface:
  display_name: "Candidate Scorer"
  short_description: "Score and rank parsed candidates against a job config"
  default_prompt: "Use $skill-scorer to score this extracted candidate profile: <extracted.json>"
