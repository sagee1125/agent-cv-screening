venv/
__pycache__/
*.pyc
.DS_Store
*.log
.env

frontend/node_modules/
frontend/.env
frontend/dist/
*.tsbuildinfo
# Local runtime output (backend scripts, eval artifacts)
backend/data/
data/

# Throwaway dev scripts created during testing
backend/tmp_*.py

# Large Docker image export artifact (temporary backup), not for version control.
cv-backend-patched.tar
.workbuddy-ai/memory/
# Release build output (asset is attached to GitHub Releases by CI).
release/dist/

# Local caches for the release self-test simulation.
release/tests/.cache/

# Local backups of the expert package (not for version control)
_expert-backup-*/
